import java.util.*;
public class inputoutputinarray {
    public static void main(String[] args) {
        int marks[] = new int[10];

        /* 

        Scanner scn = new Scanner(System.in);


        marks[0] = scn.nextInt();
        marks[1] = scn.nextInt();
        marks[2] = scn.nextInt();

        System.out.println("phy : " + marks[0]);
        System.out.println("chem : " + marks[1]);
        System.out.println("maths : " + marks[2]);


        // marks[2] = 95;                              // value can be updated
        // System.out.println("maths : " + marks[2]);



        // marks[2] = marks[2] +1;                     // operation can be done on individual element
        // System.out.println("maths : " + marks[2]);

        int percent = (marks[0]+marks[1]+marks[2])/3;        // formula can be implemented on array elements
        System.out.println(" percentage = " + percent+"%");

        */

        System.out.println(" length of array: "+ marks.length);      // arrayname.length  can be used to find length of array
    }
    
}

                // elements of array with their index can be treated as variable so they can be updated easily